    // // Quick Sort test
    // res = quickSort(unsortedArray, size);
    // printf("%d", compareArrays(res));

    // // Merge Sort test
    // res = mergeSort(unsortedArray, size);
    // printf("%d", compareArrays(res));